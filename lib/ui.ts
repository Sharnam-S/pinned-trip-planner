// The two icon steps of the trip-panel design system, mirroring the
// --fs-icon-nav / --fs-icon-card tokens in globals.css: 14px when an icon is
// chrome or sits inline beside a label, 20px when it's a card's own control.
export const ICON_NAV = 14;
export const ICON_CARD = 20;
